[www]
user = www-data
group = www-data

listen = /var/run/php/php-fpm.sock
listen.owner = www-data
listen.group = www-data
listen.mode = 0666

; CRITICAL: These 3 lines solve the status 78 crash
pm = static
pm.max_children = 10
pm.max_requests = 1000

clear_env = no